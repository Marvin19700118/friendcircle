# NetworkAI 雲端化與功能補完計畫

目前專案這是一個純前端 (Single Page Application) 的原型，所有的資料（聯絡人、對話紀錄、登入狀態）都暫存在瀏覽器的記憶體中，**重新整理頁面資料就會消失**。

為了讓這成為一個真正可用的產品，我們需要導入雲端後端服務。

## 📊 現況分析 (As-Is)
- **資料儲存**: React `useState` (記憶體)，包含 `App.tsx` 中的 `logs` 和 `DashboardScreen.tsx` 中的 `allContacts` 都是寫死的假資料。
- **身分驗證**: 模擬登入 (`handleLogin`)，無真實密碼驗證。
- **AI 整合**: 前端直接呼叫 Google Gemini API (API Key 暴露風險)。
- **圖片**: 使用 Unsplash 外部連結，無法上傳使用者自己的照片。

---

## 🚀 待完成功能清單 (To-Be)

### 1. 核心資料雲端化 (Database)
- [ ] **使用者資料庫**: 儲存使用者設定、API Key (加密)、個人偏好。
- [ ] **聯絡人管理 (CRUD)**:
  - 真正的新增、讀取、修改、刪除聯絡人。
  - 欄位擴充：增加備註、生日、標籤管理。
- [ ] **互動紀錄 (Interaction Logs)**: 將 `logs` 移至雲端，並支援與特定聯絡人關聯。

### 2. 身分驗證與安全 (Auth & Security)
- [ ] **真實註冊/登入**: 整合 OAuth (Google/Apple Login) 或 Email/Password 驗證。
- [ ] **資料隔離**: 確保 A 使用者只能看到 A 的聯絡人。
- [x] **API Key 安全**: 將 Gemini API calls 移至後端 (Serverless Function)，避免 Key 在前端外洩。

### 3. 檔案儲存 (Storage)
- [ ] **圖片上傳**: 支援聯絡人大頭貼上傳。
- [ ] **名片辨識**: 上傳名片圖片 -> OCR (Gemini) -> 自動填入欄位。

### 4. 進階 AI 功能
- [ ] **自動化建議**: 每天早上由後端排程掃描，主動推播「今天該聯絡誰」。
- [ ] **上下文記憶**: AI 聊天室需儲存歷史對話至資料庫。

---

## 📅 雲端化實作計畫 (建議使用 Firebase 或 Supabase)

這個計畫表假設我們使用 **Firebase** (Google 生態系，與 Gemini 整合性佳) 作為範例：

### 第一階段：基礎建設 (1-2 週)
1. **建立 Firebase 專案**: 設定 Firestore (資料庫) 與 Authentication (驗證)。
2. **整合 Firebase SDK**: 安裝並初始化 `firebase/app`, `firebase/auth`, `firebase/firestore`。
3. **替換登入流程**: 修改 `App.tsx` 與 `LoginScreen`，串接 Firebase Auth。

### 第二階段：資料庫串接 (2-3 週)
1. **設計資料結構 (Schema)**:
   - `users/{uid}`: 使用者資料
   - `users/{uid}/contacts/{contactId}`: 聯絡人
   - `users/{uid}/interactions/{logId}`: 互動紀錄
2. **實作 CRUD**:
   - 移除 `DashboardScreen` 的假資料 `allContacts`。
   - 建立 `ContactService` 負責讀寫 Firestore。
   - 實作「新增聯絡人」表單功能。

### 第三階段：AI 與檔案強化 (2 週)
1. **名片掃描**: 結合 `geminiService.ts` 的 OCR 功能與 Firebase Storage 圖片上傳。
2. **安全強化**: 使用 Firebase Functions (後端) 來轉發 Gemini API 請求，隱藏 API Key。

### 第四階段：部署與維運
1. **設定 CI/CD**: 確保 GitHub Action 能正確讀取 Secret 變數並部署。
2. **監控**: 設定錯誤追蹤 (Crashlytics/Sentry)。

---

## 🛠️ 下一步建議
如果您同意這個方向，建議我們先從 **「第一階段：建立 Firebase 並替換登入系統」** 開始。這會是將資料持久化的第一步。
